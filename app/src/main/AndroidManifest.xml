<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ruok">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUp" />
        <activity android:name=".activities.SignIn" />
        <activity android:name=".activities.ResetPassword" />
        <activity android:name=".activities.ProblemPage" />
        <activity android:name=".activities.RecordPage" />
        <activity android:name=".activities.CareProvider" />
        <activity android:name=".activities.PatientProfile" />
        <activity android:name=".activities.AddRecord" />
        <activity android:name=".activities.CareProviderViewListOfProblems" />
        <activity android:name=".activities.CareProviderViewListOfRecords" />
        <activity android:name=".activities.CareProviderViewRecordDetails" />
        <activity android:name=".activities.SearchRequest" />
        <activity android:name=".activities.Geolocation" />
        <activity android:name=".activities.BodyLocation"></activity>
    </application>

</manifest>