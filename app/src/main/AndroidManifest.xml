<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ruok">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.ResetPasswordActivity" />
        <activity android:name=".activities.ProblemListActivity" />
        <activity android:name=".activities.RecordPageActivity" />
        <activity android:name=".activities.CareProviderActivity" />
        <activity android:name=".activities.PatientProfileActivity" />
        <activity android:name=".activities.CareProviderViewListOfProblemsActivity" />
        <activity android:name=".activities.CareProviderViewListOfRecordsActivity" />
        <activity android:name=".activities.CareProviderViewRecordDetailsActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.GeolocationActivity" />
        <activity android:name=".activities.AddRecordActivity" />
        <activity android:name=".activities.BodyLocationActivity" />
        <activity android:name=".activities.FrontAndBackPhotoActivity"></activity>
    </application>

</manifest>